# {{ansible_managed}}
#
# Configuration snippet generated by Ansible postgres role
# from the settings in 'postgresql_conf_entries'

port = {{postgres_port}}

{% for k, v in postgresql_conf_entries.iteritems() %}
{{k}} = {{v}}
{% endfor %}
