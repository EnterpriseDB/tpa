---

- name: Install Barman package
  package: name=barman state=latest
  tags: [barman, pkg]

- name: Install Postgres client package
  package:
    name: "{{ postgres_client_packages[ansible_os_family] }}"
    state: latest
  tags: [barman, pkg]
