---

allow_duplicates: no

dependencies:
  - role: bdr/pkg
  - role: postgres/user
    tags: [postgres, user]
  - role: postgres/initdb
  - role: postgres/createuser
    username: postgres
  - role: bdr/config
  - role: sys/ssh
    ssh_user: postgres
    tags: [sys, ssh]
