---

allow_duplicates: no
dependencies:
    - role: pgbouncer/pkg
      when: >
        'pgbouncer' in role
    - role: pgbouncer/config
      when: >
        'postgres' in role or
        'pgbouncer' in role
