---

# © Copyright EnterpriseDB UK Limited 2015-2021 - All rights reserved.

- name: Install harp.ini
  template:
    src: harp.ini.j2
    dest: "{{ harp_directory }}/harp.ini"
    owner: "{{ postgres_user }}"
    group: "{{ postgres_group }}"
    mode: 0644
  notify:
    - Note HARP restart required
