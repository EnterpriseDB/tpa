---

- include_role: name=common/pkg
  tags: [common, pkg]

- name: Set the hostname
  hostname: name="{{ inventory_hostname }}"
  when:
    platform not in ['docker']
  tags: [common, hostname]

- include_tasks: tpa.yml
  tags: common
