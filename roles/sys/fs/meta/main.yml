---

# Copyright © 2ndQuadrant Limited <info@2ndquadrant.com>

allow_duplicates: yes
dependencies:
  - role: secret
    vars:
      secret_name: "{{ encryption }}_passphrase"
    when: >
      encryption|default('none') != 'none'
