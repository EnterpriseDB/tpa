$FileOwner {{ postgres_user }}
$FileGroup {{ postgres_group }}
if $programname == 'postgres' then ?LOGPERHOST
if $programname == 'pgbouncer' then ?LOGPERHOST
if $programname == 'repmgr' then ?LOGPERHOST
