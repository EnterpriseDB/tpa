---

- name: Add rsyslog rules for remote logging
  template:
    src: 99-tpa-node.conf.j2
    dest: /etc/rsyslog.d/99-tpa-node.conf
    owner: root
    group: root
    mode: 0644
  notify:
    - Restart rsyslog

- meta: flush_handlers
