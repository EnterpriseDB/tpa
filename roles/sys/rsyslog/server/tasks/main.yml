---

- name: Enable TCP syslog reception
  copy:
    src: 00-enable-tcp-reception.conf
    dest: /etc/rsyslog.d/00-enable-tcp-reception.conf
  notify:
    - Restart rsyslog

- name: Enable TCP syslog reception
  copy:
    src: 22-postgres.conf
    dest: /etc/rsyslog.d/22-postgres.conf
  notify:
    - Restart rsyslog

- meta: flush_handlers
