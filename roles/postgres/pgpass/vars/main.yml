---

# © Copyright EnterpriseDB UK Limited 2015-2025 - All rights reserved.

_pgpassfile: "{{ pgpassfile|default('~%s/.pgpass' % postgres_user) }}"
_pgpass_owner: "{{ pgpass_owner|default(postgres_user) }}"
_pgpass_group: "{{ pgpass_group|default(postgres_group) }}"
_pgpass_host: "{{ pgpass_host|default('*') }}"
_pgpass_users: "{{ pgpass_users|default(default_pgpass_users) }}"
_ssl_client_cert_dir: "{{ ssl_client_cert_dir|default('/') }}"
