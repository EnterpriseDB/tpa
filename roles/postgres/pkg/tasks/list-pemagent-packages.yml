---

# © Copyright EnterpriseDB UK Limited 2015-2023 - All rights reserved.

- name: "Add pem-agent packages"
  include_role: name=pkg/add_to_list
  vars:
    list_contents: "{{[postgres_extensions_dictionary.sql_profiler.packages[postgres_flavour][ansible_distribution], postgres_extensions_dictionary.edb_wait_states.packages[postgres_flavour][ansible_distribution]]|flatten}}"
  when: "'postgres' in role and 'pem-agent' in role"
