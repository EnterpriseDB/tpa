---

# © Copyright EnterpriseDB UK Limited 2015-2023 - All rights reserved.

- include_role:
   name: "postgres/{{ subrole }}"
   tasks_from: "{{ run_major_upgrade|ternary('main.yml','update.yml') }}"
  vars:
    subrole: "{{ run_major_upgrade|ternary('upgrade','pkg') }}"
    run_major_upgrade: "{{ postgres_major_version_upgrade is defined|ternary(postgres_major_version_upgrade,false) }}"

