---

- include: stop_postgres.yml
  when: postgres_cleanup

- include: postgres_install_git.yml tags=postgres_install
  when: postgres_install and not postgres_cleanup_only

- include: initdb.yml tags=postgres_initdb
  when: postgres_install and not postgres_cleanup_only
