---

# Copyright © 2ndQuadrant Limited <info@2ndquadrant.com>

- include_role: name=postgres/install
- include_role: name=postgres/user
  tags: [postgres, user]
- include_role: name=postgres/initdb
  tags: always
- include_role:
    name: postgres/config
    apply:
      tags: [postgres, config]
  when: >
    pgdata_initialised
  tags: always
