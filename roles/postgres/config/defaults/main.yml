postgres_port: 5432

# By default, we leave pg_hba.conf alone, but the caller can define an
# optional template or just an array of lines to put in pg_hba.conf
# instead.

postgres_hba_template: ''
postgres_hba_settings: []

# By default, we leave postgresql.conf alone, but the caller can define
# an optional template. Any extra settings in the supplied hash will be
# included via include_dir.

postgres_config_template: ''
postgres_config_settings: {}
