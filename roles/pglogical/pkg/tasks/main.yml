---

- include: "os/{{ ansible_os_family }}.yml"
  tags: [pglogical, pkg]

- name: Install pglogical packages
  package:
    name: "{{ pglogical_packages[ansible_distribution] }}"
    state: latest
  tags: [pglogical, pkg]
