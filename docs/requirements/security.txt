* DBAFirewall installed
* SQLFirewall installed
* passwordcheck contrib installed
* pgaudit installed
* logforward installed
* audit events forwarded to audit database on separate node, protected by
  replication and backup
* audit records written to database using blockchain hash, so you can't tamper
  with records without changing all future records
* Use AES256
