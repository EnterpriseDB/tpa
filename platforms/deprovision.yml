---

# Decommission an already-provisioned cluster:
#
#     bin/deprovision clustername

- name: Deprovision cluster
  hosts: localhost
  tasks:
    - import_tasks: load-config.yml
      tags: always

    - import_tasks: validate.yml
      tags: always

    - include_tasks: "{{ item }}/deprovision.yml"
      with_items:
        - "{{ cluster_platforms }}"
        - common
