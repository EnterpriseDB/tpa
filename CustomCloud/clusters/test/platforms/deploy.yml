---

- name: Set up TPA cluster nodes
  hosts: tag_Name_TestPlatforms
  gather_facts: no
  sudo_user: root
  sudo: true

  vars:
    pgversion: 9.5

  pre_tasks:
    - include: ../../../platforms/aws/hostvars.yml

    - name: Set the cluster_hostname for each node
      set_fact:
        cluster_hostname: "tpa-{{ ec2_tag_node }}"
      tags: always

  roles:
    - role: common

    - role: sys/tune
      tags: always

    - role: sys/sysctl
      tags: sysctl

    - role: sys/fs
      device: /dev/xvdb
      mountpoint: /var/lib/postgresql
      tags: fs

    - role: postgres/pkg
      tags: postgres

    - role: sys/ssh
      user: postgres
      tags: ssh
