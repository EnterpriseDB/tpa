#!/bin/sh

repmgr standby promote -f "{{ repmgr_conf_dir }}/repmgr.conf"

# On successful promotion, failover the elastic IP to itself

if [ "$?" -eq "0" ]; then
	python2.7 "{{ repmgr_conf_dir }}/assign_eip.py"
fi
