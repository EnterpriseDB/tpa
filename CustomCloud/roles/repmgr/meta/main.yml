---

dependencies:
  - role: secret
    vars:
      secret_name: repmgr_password
  - role: repmgr/pkg
    when: >
      'postgres' in role
  - role: repmgr/common
    when: >
      'postgres' in role
  - role: repmgr/primary
    when: >
      'primary' in role
  - role: repmgr/replica
    when: >
      'replica' in role
  - role: repmgr/witness
    when: >
      'witness' in role
