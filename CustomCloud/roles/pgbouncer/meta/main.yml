---

allow_duplicates: yes
dependencies:
    - { role: pgbouncer/pkg }
    - { role: postgres/createuser, username: 'pgbouncer',  when: 'bdr' in role or 'primary' in role }
    - { role: pgbouncer/config, when: 'pgbouncer' in role }
    - { role: pgbouncer/final, when: 'pgbouncer' in role }
