---

- include_vars: "{{ playbook_dir }}/vault/{{ name }}_password.yml"

- name: Create the {{ user }} user
  postgresql_user:
    name: "{{ name }}"
    password: "{{ vars[name+'_password'] }}"
    role_attr_flags: superuser
  become_user: postgres
  become: yes
