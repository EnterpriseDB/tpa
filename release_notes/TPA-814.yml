- summary: set pem_python_executable outside pkg role
  description: |
    Fixed a bug whereby if the user excluded the `pkg` selector, later
    pem-related tasks would fail because the pem_python_executable fact
    had not been set.
  # Possible types: notable_change, minor_change, bugfix or breaking_change
  type: bugfix
  # At least one between jira_tickets or support_tickets must be defined. You
  # can define both if you want
  # You must have at least one list item in each *_tickets section that is
  # defined
  jira_tickets:
  - TPA-814
