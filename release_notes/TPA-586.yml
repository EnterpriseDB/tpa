- summary: Enable EFM probes when a PEM agent is registered on an EFM node
  description: |
    The `--efm-install-path` and `--efm-cluster-name` flags are set when a 
    PEM server is registered on an EFM node. 

    The `Streaming Replication`, `Failover Manager Node Status` and 
    `Failover Manager Cluster Info` probes are enabled when a PEM agent is 
    registered on an EFM node.
  # Possible types: notable_change, minor_change, bugfix or breaking_change
  type: minor_change
  # At least one between jira_tickets or support_tickets must be defined. You
  # can define both if you want
  # You must have at least one list item in each *_tickets section that is
  # defined
  jira_tickets:
  - TPA-586