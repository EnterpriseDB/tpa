- summary: Raise an ArchitectureError when BDR-Always-ON is configured with BDR version 5
  description: |
    An architecture error is raised during `tpaexec configure` if
    `--bdr-version 5` is passed with `-a BDR-Always-ON` alerting
    the user that BDR version 5 should be used with `PGD-Always-ON`. 
  # Possible types: notable_change, minor_change, bugfix or breaking_change
  type: minor_change
  # At least one between jira_tickets or support_tickets must be defined. You
  # can define both if you want
  # You must have at least one list item in each *_tickets section that is
  # defined
  jira_tickets:
  - TPA-742
