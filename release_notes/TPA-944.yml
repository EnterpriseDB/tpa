- summary: Fix `patronictl switchover` command usage
  description: |
    Patroni moved out from `master` naming some months ago
    TPA will now correctly use `--leader` instead of deprecated `--master`
    parameter when using `patronictl switchover` command.
# Possible types: notable_change, minor_change, bugfix or breaking_change
  type: bugfix
# At least one between jira_tickets or support_tickets must be defined. You
# can define both if you want
# You must have at least one list item in each *_tickets section that is
# defined
  jira_tickets:
  - TPA-944
