- summary: Add `PGPORT` to default `.bashrc` installed by TPA
  description: |
  The `PGPORT` environment variable has been exported as part of the 
  default `.bashrc`, and defaults to the port value used by the selected
  `postgres_flavour`, or `postgres_port` if specified in the config file.
    
  # Possible types: notable_change, minor_change, bugfix or breaking_change
  type: minor_change
  # At least one between jira_tickets or support_tickets must be defined. You
  # can define both if you want
  # You must have at least one list item in each *_tickets section that is
  # defined
  jira_tickets:
  - TPA-811
