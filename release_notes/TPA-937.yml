- summary: Count instances correctly in PEM clusters
  description: |
    Fixed a bug whereby in certain circumstances, TPA would incorrectly
    calculate the number of instances in a BDR-Always-ON cluster with a
    PEM server, causing "tpaexec configure" to fail with "StopIteration".
  # Possible types: notable_change, minor_change, bugfix or breaking_change
  type: bugfix
  # At least one between jira_tickets or support_tickets must be defined. You
  # can define both if you want
  # You must have at least one list item in each *_tickets section that is
  # defined
  jira_tickets:
  - TPA-937
