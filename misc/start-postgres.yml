---

- name: Start_Stop PG on X2 cluster nodes
  hosts: tag_Name_X2
  gather_facts: no
  remote_user: admin
  sudo_user: root
  sudo: true

  roles:
    - role: start-pg
      luks_volume: postgres
      luks_device: /dev/xvdb
      luks_mountpoint: /var/lib/postgresql
      tags: start-pg
