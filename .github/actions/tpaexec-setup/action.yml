# © Copyright EnterpriseDB UK Limited 2015-2023 - All rights reserved.

name: "TPAexec setup"
description: "Setup TPAexec virtual environment and package cache"

runs:
  using: "composite"
  steps:
    - name: Run tpaexec setup
      shell: bash
      run: ./bin/tpaexec setup --pip-cache-dir ~/.cache/pip

outputs: {}
